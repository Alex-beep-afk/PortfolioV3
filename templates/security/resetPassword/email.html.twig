<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Réinitialisation de votre mot de passe</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            line-height: 1.6;
            color: #1f2937;
            background-color: #f3f4f6;
            margin: 0;
            padding: 0;
        }
        .email-container {
            max-width: 600px;
            margin: 40px auto;
            background: linear-gradient(to bottom right, #0f172a, #1e293b);
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 4px 20px rgba(6, 182, 212, 0.2);
        }
        .email-header {
            background: linear-gradient(135deg, rgba(6, 182, 212, 0.2), rgba(168, 85, 247, 0.2));
            padding: 30px;
            text-align: center;
            border-bottom: 1px solid rgba(6, 182, 212, 0.3);
        }
        .email-header h1 {
            color: #22d3ee;
            font-size: 24px;
            font-weight: bold;
            margin: 0;
            text-shadow: 0 0 20px rgba(6, 182, 212, 0.8);
        }
        .email-body {
            background-color: #1e293b;
            padding: 40px 30px;
            color: #e5e7eb;
        }
        .email-content {
            background-color: rgba(31, 41, 55, 0.5);
            border: 1px solid rgba(6, 182, 212, 0.3);
            border-radius: 8px;
            padding: 30px;
            margin-bottom: 20px;
        }
        .reset-button {
            display: inline-block;
            background: linear-gradient(135deg, rgba(6, 182, 212, 0.5), rgba(168, 85, 247, 0.5));
            color: #22d3ee;
            padding: 14px 28px;
            text-decoration: none;
            border-radius: 8px;
            font-weight: 600;
            border: 1px solid rgba(6, 182, 212, 0.5);
            margin: 20px 0;
            transition: all 0.3s ease;
            box-shadow: 0 0 15px rgba(6, 182, 212, 0.3);
        }
        .reset-button:hover {
            background: linear-gradient(135deg, rgba(6, 182, 212, 0.7), rgba(168, 85, 247, 0.7));
            box-shadow: 0 0 20px rgba(6, 182, 212, 0.5);
        }
        .reset-link {
            word-break: break-all;
            color: #67e8f9;
            text-decoration: underline;
        }
        .email-footer {
            background-color: #0f172a;
            padding: 20px;
            text-align: center;
            border-top: 1px solid rgba(6, 182, 212, 0.3);
            color: #9ca3af;
            font-size: 12px;
        }
        .info-box {
            background-color: rgba(168, 85, 247, 0.1);
            border: 1px solid rgba(168, 85, 247, 0.3);
            border-radius: 8px;
            padding: 15px;
            margin: 20px 0;
            color: #c4b5fd;
        }
        .icon {
            display: inline-block;
            width: 20px;
            height: 20px;
            margin-right: 8px;
            vertical-align: middle;
        }
    </style>
</head>
<body>
    <div class="email-container">
        <div class="email-header">
            <h1>🔐 Réinitialisation de mot de passe</h1>
        </div>
        
        <div class="email-body">
            <div class="email-content">
                <p style="font-size: 16px; margin-bottom: 20px;">Bonjour,</p>
                
                <p style="margin-bottom: 20px;">
                    Vous avez demandé à réinitialiser votre mot de passe. Cliquez sur le bouton ci-dessous pour procéder à la réinitialisation :
                </p>
                
                <div style="text-align: center; margin: 30px 0;">
                    <a href="{{ url('reset.password.reset', {token: resetToken.token}) }}" class="reset-button">
                        Réinitialiser mon mot de passe
                    </a>
                </div>
                
                <p style="margin-top: 20px; font-size: 14px; color: #9ca3af;">
                    Ou copiez-collez ce lien dans votre navigateur :
                </p>
                <p style="margin: 10px 0;">
                    <a href="{{ url('reset.password.reset', {token: resetToken.token}) }}" class="reset-link">
                        {{ url('reset.password.reset', {token: resetToken.token}) }}
                    </a>
                </p>
                
                <div class="info-box">
                    <p style="margin: 0; font-size: 14px;">
                        <strong>⏰ Important :</strong> Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
                    </p>
                </div>
                
                <p style="margin-top: 30px; font-size: 14px; color: #9ca3af;">
                    Si vous n'avez pas demandé cette réinitialisation, vous pouvez ignorer cet email en toute sécurité.
                </p>
            </div>
        </div>
        
        <div class="email-footer">
            <p style="margin: 0;">Cordialement,<br>L'équipe Portfolio</p>
        </div>
    </div>
</body>
</html>
