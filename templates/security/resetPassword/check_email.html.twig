{% extends 'base.html.twig' %}

{% block title %}Email de réinitialisation envoyé - Portfolio{% endblock %}

{% block body %}

<main class="min-h-screen flex items-center justify-center px-4 sm:px-6 lg:px-8" style="background: linear-gradient(to bottom right, #0f172a, #1e293b);" data-turbo="false">
    <div class="w-full max-w-md">
        <!-- Check Email Card -->
        <section class="bg-gray-800/50 rounded-xl border border-cyan-500/30 backdrop-blur-sm overflow-hidden" style="box-shadow: 0 4px 20px rgba(6, 182, 212, 0.2);">
            <header class="p-6 border-b border-cyan-500/30" style="background: linear-gradient(135deg, rgba(6, 182, 212, 0.2), rgba(168, 85, 247, 0.2));">
                <div class="flex items-center justify-center mb-2">
                    <div class="bg-white/10 rounded-lg p-3" style="box-shadow: 0 0 15px rgba(6, 182, 212, 0.4);">
                        <svg class="w-8 h-8 text-cyan-400" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
                        </svg>
                    </div>
                </div>
                <h1 class="text-2xl font-bold text-cyan-400 text-center" style="text-shadow: 0 0 20px rgba(6, 182, 212, 0.8);">Email envoyé</h1>
                <p class="mt-2 text-sm text-gray-400 text-center">Vérifiez votre boîte de réception</p>
            </header>
            
            <div class="p-6">
                <div class="bg-cyan-500/10 border border-cyan-500/30 rounded-lg p-6 mb-6" style="box-shadow: 0 0 15px rgba(6, 182, 212, 0.2);">
                    <div class="flex items-start">
                        <svg class="w-6 h-6 text-cyan-400 mr-3 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                        </svg>
                        <div>
                            <p class="text-sm text-gray-300 leading-relaxed">
                                Si un compte correspondant à votre adresse email existe, un email contenant un lien de réinitialisation vient d'être envoyé.
                            </p>
                            <p class="text-sm text-cyan-400 mt-3 font-medium">
                                Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
                            </p>
                        </div>
                    </div>
                </div>

                <div class="bg-purple-500/10 border border-purple-500/30 rounded-lg p-4 mb-6">
                    <div class="flex items-start">
                        <svg class="w-5 h-5 text-purple-400 mr-2 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                        </svg>
                        <p class="text-sm text-gray-400">
                            Si vous ne recevez pas d'email, vérifiez votre dossier spam ou 
                            <a href="{{ path('forgot.password.request') }}" class="text-cyan-400 hover:text-cyan-300 transition-colors underline decoration-cyan-500/50 hover:decoration-cyan-400 font-medium">
                                réessayez
                            </a>.
                        </p>
                    </div>
                </div>

                <!-- Back to Login Link -->
                <div class="pt-4 text-center">
                    <a href="{{ path('login') }}" class="inline-flex items-center text-sm text-cyan-400 hover:text-cyan-300 transition-colors underline decoration-cyan-500/50 hover:decoration-cyan-400">
                        <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
                        </svg>
                        Retour à la connexion
                    </a>
                </div>
            </div>
        </section>
    </div>
</main>
{% endblock %}
